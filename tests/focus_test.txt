You are looking at a Smart TV application UI.

Exactly ONE UI element is currently focused.
Focused elements have a visually distinct background or emphasis
(e.g. white background, highlighted container, enlarged tile).

Your task:
1. Identify which UI element is currently focused.
2. Return its label/text if visible.
3. Return an approximate bounding box.

Rules:
- Respond ONLY in valid JSON.
- Do NOT describe the image.
- Do NOT give navigation advice.

JSON format:
{
  "focused_element": {
    "label": "<string or null>",
    "bbox": [x, y, width, height],
    "confidence": 0.0-1.0
  }
}
